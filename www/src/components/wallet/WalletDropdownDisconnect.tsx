import { ReactNode } from 'react';
// Placeholder for WalletDropdownDisconnect UI
export function WalletDropdownDisconnect({ className }: { className?: string }) {
  return <button className={className}>Disconnect</button>;
}
