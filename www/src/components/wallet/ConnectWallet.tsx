import { ReactNode } from 'react';
// Placeholder for ConnectWallet UI
export function ConnectWallet({ children }: { children?: ReactNode }) {
  return <button>Connect Wallet (placeholder)</button>;
}
